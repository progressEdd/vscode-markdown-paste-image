---
phase: 02-error-handling-bugfix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/extension.ts]
autonomous: false
requirements: [BUG-01]
user_setup: []

must_haves:
  truths:
    - "No 'rejected promise not handled' warnings appear in console"
    - "Context menu 'Markdown Paste' works correctly and executes the paste operation"
    - "All paste commands (MarkdownPaste, MarkdownDownload, MarkdownPasteCode) execute without errors"
    - "Insert Math Symbol command handles quick pick cancellation gracefully"
    - "All commands are properly registered with context.subscriptions for disposal"
  artifacts:
    - path: "src/extension.ts"
      provides: "Command handlers with proper promise handling"
      contains: "async () => { try { await"
      min_lines: 80
  key_links:
    - from: "Command handlers"
      to: "Paster async methods"
      via: "await keyword"
      pattern: "await Paster\\.(paste|pasteDownload|pasteCode)\\(\\)"
    - from: "showQuickPick()"
      to: "Error handling"
      via: "try/catch in async handler"
      pattern: "try \\{[\\s\\S]*showQuickPick"
    - from: "Command registrations"
      to: "context.subscriptions"
      via: "context.subscriptions.push()"
      pattern: "context\\.subscriptions\\.push\\("
---

<objective>
Fix unhandled promise rejections in all command handlers to eliminate console warnings and ensure context menu functionality works correctly.

Purpose: Unhandled promise rejections cause VSCode Extension Host warnings and break context menu functionality. The "Markdown Paste" command fails silently when invoked from the editor context menu, impacting user experience.

Output: All command handlers properly await async operations and handle errors gracefully, with all commands registered for proper disposal.
</objective>

<execution_context>
@/home/progressedd/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/progressedd/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-error-handling-bugfix/02-CONTEXT.md
@.planning/phases/02-error-handling-bugfix/02-RESEARCH.md

<interfaces>
<!-- Key interfaces and patterns the executor needs -->

From src/extension.ts (current implementation):

- LatexSymbol class with insertToEditor() method that has nested editor.edit() promises
- Command registrations at lines 49-75
- telesoho.insertMathSymbol NOT in context.subscriptions (line 49)
- Other commands ARE in context.subscriptions (lines 56-75)

From src/paster.ts:

- paste() is async (line 84): `public static async paste()`
- pasteDownload() is async (line 138): `public static async pasteDownload()`
- pasteCode() is async (line 29): `public static async pasteCode()`
- Ruby() is NOT async (line 158): `public static Ruby()` - returns void

From src/Logger.ts:

- Logger.log(...message: any[]) - for general logging
- Logger.showErrorMessage(message: string) - for user-facing errors
- Logger.showInformationMessage(message: string) - for user-facing info

VSCode Extension Best Practices:

- All async operations should use async/await with try/catch
- All command registrations should be added to context.subscriptions
- editor.edit() returns Thenable<boolean> and should be awaited
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix promise handling in all command handlers</name>
  <files>src/extension.ts</files>
  <behavior>
    - Test 1: All async Paster methods are awaited (paste, pasteDownload, pasteCode)
    - Test 2: showQuickPick() errors are caught and handled
    - Test 3: insertToEditor() nested editor.edit() promises are properly chained
    - Test 4: All commands registered with context.subscriptions
  </behavior>
  <action>
Fix all unhandled promise rejections in src/extension.ts:

**1. Fix telesoho.insertMathSymbol (lines 49-55):**

- Convert command handler to async function
- Add try/catch for showQuickPick() cancellation
- Log errors via Logger.log() (no user-facing messages for cancellation)
- Add command to context.subscriptions
- Fix insertToEditor() method (lines 23-41):
  - Convert to async method
  - Await the first editor.edit() call
  - Await the second editor.edit() call
  - Add error handling with Logger.log()

**2. Fix telesoho.MarkdownPaste (lines 62-64):**

- Convert command handler to async function
- Add try/catch block
- Await Paster.paste()
- Log errors via Logger.log()

**3. Fix telesoho.MarkdownDownload (lines 57-59):**

- Convert command handler to async function
- Add try/catch block
- Await Paster.pasteDownload()
- Log errors via Logger.log()

**4. Fix telesoho.MarkdownPasteCode (lines 72-74):**

- Convert command handler to async function
- Add try/catch block
- Await Paster.pasteCode()
- Log errors via Logger.log()

**5. telesoho.MarkdownRuby (lines 67-69):**

- Already correct - Paster.Ruby() is not async
- Keep as-is

**Pattern to follow:**

```typescript
context.subscriptions.push(
  vscode.commands.registerCommand("telesoho.CommandName", async () => {
    try {
      await Paster.asyncMethod();
    } catch (error) {
      Logger.log("Command failed:", error);
    }
  })
);
```

**For insertToEditor method:**

```typescript
public async insertToEditor(item: vscode.QuickPickItem) {
  if (!item) {
    return;
  }
  const editor = vscode.window.activeTextEditor;
  if (!editor) {
    return;
  }

  try {
    await editor.edit((editBuilder) => {
      editBuilder.delete(editor.selection);
    });
    await editor.edit((editBuilder) => {
      editBuilder.insert(editor.selection.start, item.description);
    });
  } catch (error) {
    Logger.log("Failed to insert math symbol:", error);
  }
}
```

**Constraints:**

- Maintain existing behavior (no functional changes)
- Use Logger.log() for error logging
- No user-facing error messages for quick pick dismissal (user intentionally cancelled)
- Use `bun` instead of `npm` for build commands
  </action>
  <verify>
  <automated>bun run compile</automated>
  </verify>
  <done> - All command handlers use async/await with try/catch - All async Paster methods are awaited - insertToEditor properly chains editor.edit() promises - All commands registered with context.subscriptions - TypeScript compiles without errors
  </done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify fixed commands work correctly</name>
  <what-built>Fixed all unhandled promise rejections in command handlers</what-built>
  <how-to-verify>
**Step 1: Build the extension**
```bash
bun run compile
```

**Step 2: Reload the VSCode window**

- Press `Ctrl+Shift+P` (or `Cmd+Shift+P` on Mac)
- Type "Reload Window" and press Enter

**Step 3: Test the fixed commands**

1. **Test Context Menu (CRITICAL):**

   - Open a Markdown file
   - Copy some text to clipboard
   - Right-click in the editor
   - Click "Markdown Paste" from context menu
   - **Expected:** Text pastes correctly, no console errors

2. **Test Keyboard Shortcuts:**

   - Press `Ctrl+Alt+V` (Markdown Paste)
   - **Expected:** Pastes clipboard content, no errors
   - Press `Ctrl+Alt+C` (Markdown Paste Code)
   - **Expected:** Pastes as code block, no errors
   - Press `Ctrl+Alt+D` (Markdown Download)
   - **Expected:** Handles URL download, no errors

3. **Test Insert Math Symbol:**

   - Press `Ctrl+Alt+\` (or `Cmd+Alt+\` on Mac)
   - **Expected:** Quick pick appears
   - Press Escape to cancel
   - **Expected:** No errors in console, no warnings
   - Press `Ctrl+Alt+\` again
   - Select a symbol
   - **Expected:** Symbol inserts at cursor position

4. **Check Console:**
   - Open Developer Tools: `Help > Toggle Developer Tools`
   - Go to Console tab
   - **Expected:** No "rejected promise not handled" warnings
   - **Expected:** No red error messages from the extension

**Success Criteria:**

- All commands work without console errors
- Context menu "Markdown Paste" executes successfully
- No "rejected promise not handled" warnings
- Quick pick cancellation handled gracefully
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
  </task>

</tasks>

<verification>
**Build Verification:**
- TypeScript compiles without errors: `bun run compile`
- No linting errors: `bun run lint`

**Manual Verification (checkpoint):**

- Context menu "Markdown Paste" works correctly
- All paste commands execute without errors
- Insert Math Symbol handles cancellation gracefully
- No console warnings or errors
  </verification>

<success_criteria>

- [x] All command handlers use async/await with try/catch
- [x] All async Paster methods are awaited
- [x] insertToEditor properly chains editor.edit() promises
- [x] All commands registered with context.subscriptions
- [x] TypeScript compiles without errors
- [x] No "rejected promise not handled" warnings in console
- [x] Context menu "Markdown Paste" works correctly
- [x] All paste commands execute without errors
      </success_criteria>

<output>
After completion, create `.planning/phases/02-error-handling-bugfix/02-01-SUMMARY.md`
</output>
