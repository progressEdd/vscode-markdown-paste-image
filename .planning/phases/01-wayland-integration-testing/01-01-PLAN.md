---
phase: 01-wayland-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitmodules
  - xclip/
autonomous: true
requirements:
  - SUBMODULE-01
  - SUBMODULE-02
  - SUBMODULE-03
must_haves:
  truths:
    - "xclip submodule points to progressEdd/xclip wayland-clipboard-support branch"
    - "Submodule clones successfully with wl-copy/wl-paste support"
    - "Extension compiles without errors after submodule update"
  artifacts:
    - path: "xclip/src/clipboard/wayland.ts"
      provides: "WaylandClipboard implementation"
      contains: "class WaylandClipboard"
    - path: "xclip/src/os.ts"
      provides: "Display server detection"
      contains: "detectDisplayServer"
  key_links:
    - from: ".gitmodules"
      to: "progressEdd/xclip"
      via: "submodule URL"
      pattern: "url = git@github.com:progressEdd/xclip.git"
    - from: "src/paster.ts"
      to: "xclip module"
      via: "import * as xclip"
      pattern: "import \\* as xclip from \"xclip\""
---

<objective>
Update xclip git submodule from telesoho/xclip to progressEdd/xclip wayland-clipboard-support branch, pinning to specific commit 55c32aa9a8555949fbbeef9ebb742f6b58d3252f. This enables Wayland clipboard support via wl-copy/wl-paste commands.

Purpose: Provide the foundation for Wayland clipboard operations that subsequent tests will validate.
Output: Updated submodule with Wayland support, extension compiles successfully.
</objective>

<execution_context>
@/home/progressedd/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/progressedd/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-wayland-integration-testing/01-CONTEXT.md
@.planning/phases/01-wayland-integration-testing/01-RESEARCH.md

## Current State

**Submodule status:**

- Current URL: https://github.com/telesoho/xclip.git (from .gitmodules)
- Target: git@github.com:progressEdd/xclip.git, wayland-clipboard-support branch
- Target commit: 55c32aa9a8555949fbbeef9ebb742f6b58d3252f
- Current submodule directory: empty (not initialized)

**Package dependency:**

- package.json has xclip ^1.0.7 as npm dependency
- Submodule takes precedence when TypeScript path resolution is configured
- May need to verify no conflict between npm package and submodule

## Key Decisions (LOCKED from CONTEXT.md)

- Pin to specific tested commit hash on wl-copy-support branch (not branch tip)
- Use bun instead of npm for package management
- Verify submodule works before committing update
- Run integration tests to verify after update
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update xclip submodule URL and checkout Wayland commit</name>
  <files>.gitmodules, xclip/</files>
  <action>
Update the xclip submodule to point to progressEdd/xclip wayland-clipboard-support branch at commit 55c32aa9a8555949fbbeef9ebb742f6b58d3252f.

**Steps:**

1. Deinitialize current submodule (if any): `git submodule deinit -f xclip`
2. Update .gitmodules URL: Change from `https://github.com/telesoho/xclip.git` to `git@github.com:progressEdd/xclip.git`
3. Sync submodule URL: `git submodule sync`
4. Initialize and update submodule: `git submodule update --init xclip`
5. Checkout the wayland-clipboard-support branch: `cd xclip && git checkout wayland-clipboard-support`
6. Pin to specific commit: `git checkout 55c32aa9a8555949fbbeef9ebb742f6b58d3252f`
7. Verify the commit hash matches: `cd xclip && git rev-parse HEAD` should return 55c32aa9a8555949fbbeef9ebb742f6b58d3252f

**Important:**

- Do NOT use branch tip (avoid unexpected changes from future commits)
- Verify xclip/src/clipboard/wayland.ts exists after checkout
- Verify xclip/src/os.ts has detectDisplayServer function

**Why pin to commit:** Reproducible builds, stable reference for review, not affected by future changes to branch.
</action>
<verify>
<automated>test -f xclip/src/clipboard/wayland.ts && test -f xclip/src/os.ts && grep -q "WaylandClipboard" xclip/src/clipboard/wayland.ts && grep -q "detectDisplayServer" xclip/src/os.ts</automated>
</verify>
<done>

- .gitmodules updated to git@github.com:progressEdd/xclip.git
- xclip/ directory contains wayland-clipboard-support branch at commit 55c32aa
- Files xclip/src/clipboard/wayland.ts and xclip/src/os.ts exist
  </done>
  </task>

<task type="auto">
  <name>Task 2: Build submodule and verify extension compiles</name>
  <files>xclip/, out/</files>
  <action>
Build the xclip submodule and verify the extension compiles without errors.

**Steps:**

1. Install xclip submodule dependencies using bun: `cd xclip && bun install`
2. Build xclip: `cd xclip && bun run build`
3. Verify xclip build output exists: Check for dist/ or out/ directory in xclip/
4. Return to project root and install extension dependencies: `bun install`
5. Build extension: `bun run compile`
6. Verify no TypeScript compilation errors

**Expected behavior:**

- xclip builds successfully with Wayland support
- Extension compiles without "Cannot find module 'xclip'" errors
- TypeScript resolves xclip import to the submodule (not npm package)

**If npm package conflict:**

- The submodule should take precedence via TypeScript paths
- If issues arise, verify tsconfig.json module resolution
  </action>
  <verify>
  <automated>bun run compile 2>&1 | grep -c "error TS" | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
- xclip submodule builds successfully
- Extension compiles with zero TypeScript errors
- Ready for integration test development
  </done>
  </task>

</tasks>

<verification>
1. Submodule points to correct commit: `cd xclip && git rev-parse HEAD` returns 55c32aa9a8555949fbbeef9ebb742f6b58d3252f
2. Wayland files present: `ls xclip/src/clipboard/wayland.ts xclip/src/os.ts`
3. Extension compiles: `bun run compile` exits with code 0
4. xclip build: `ls xclip/dist/` or `ls xclip/out/` shows compiled files
</verification>

<success_criteria>

- [ ] .gitmodules updated to git@github.com:progressEdd/xclip.git
- [ ] xclip submodule at commit 55c32aa9a8555949fbbeef9ebb742f6b58d3252f
- [ ] xclip/src/clipboard/wayland.ts exists with WaylandClipboard class
- [ ] xclip/src/os.ts exists with detectDisplayServer function
- [ ] Extension compiles with `bun run compile` - zero TypeScript errors
- [ ] All files committed to git
      </success_criteria>

<output>
After completion, create `.planning/phases/01-wayland-integration-testing/01-01-SUMMARY.md`
</output>
